# nunchux notes

Nice usages?

- Calendar. Done, used calcurses.
- lots of system utils
- hacker news
- reddit
- Spotify?

Config browser. Should be implemented as a directory browser so it can be used
as a more general purpose.

Let's add some caching. If we just save the menu from last time then it will be
fine. Then we can run the menu build in the background and once we have that new
menu, we should be able to substitutee b it in place of the old one.
