# Test config for environment variable inheritance integration tests
#
# These apps are designed for automated testing - they write env to files
# and exit without waiting for user input.
#
# The test script sets NUNCHUX_TEST_ID before running these apps.

[settings]
popup_width = 50%
popup_height = 50%

# Test app for popup mode - writes env to file and exits quickly
[app:env-popup]
cmd = bash -c 'env | sort > /tmp/nunchux-integration-popup'
desc = Popup env test (writes to /tmp)

# Test app for window mode - writes env to file, sleeps briefly for verification
[app:env-window]
cmd = bash -c 'env | sort > /tmp/nunchux-integration-window && sleep 2'
desc = Window env test (writes to /tmp)

# vim: ft=dosini ts=2 sw=2 et
